#include<stdio.h>

int main()
{
    int m, a[m], count;
    scanf("%d", &m);
    //scanf("%d ", &a[m]);
    for(int i=0; i<m; i++)
    {
    m-1;
    m-2;
    if(m==0 || m==1)
    {
       count++; 
    }
    }
printf("%d", count);

}